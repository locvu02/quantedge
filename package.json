{
  "private": true,
  "name": "quantedge-root",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev"
  },
  "dependencies": {
    "next": "^15.0.0"
  }
}
